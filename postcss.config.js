// With Tailwind CSS v4 and @tailwindcss/vite, this file might not be needed
// as the Vite plugin handles PostCSS setup, including Tailwind and Autoprefixer.
// If CSS issues persist after this change, try deleting this file.
export default {
  plugins: {
  },
}
